# zembra-cli

A command-line note application built with Python, Typer, Rich, and uv.

## Development

Install dependencies:

```bash
uv sync
```

Run the CLI:

```bash
uv run zembra --help
```

Run tests:

```bash
uv run pytest
```
